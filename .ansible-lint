# .ansible-lint
exclude_paths:
  - .cache/ # implicit unless exclude_paths is defined in config
  - .github/
  - requirements.yml
  - molecule_multipass/cookiecutter/{{*

skip_list:
  # Temporary skips made during migration
  - args[module]
